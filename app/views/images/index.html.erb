<% @images.each do |image| %>
<section class="row">
  <div class="col-xs-7">
    <%= link_to image_tag(image.image_url, class: "img-responsive"), image %>
  </div>
  <div class="col-xs-5">
    <h2><%= link_to image.title, image %></h2>
  </div>
</section>
<% end %>